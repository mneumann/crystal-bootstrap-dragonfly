# Crystal Bootstrap for DragonFly

The Github action `.github/workflows/release.yml` builds a `crystal.o` for
DragonFly that can then be linked to a DragonFly crystal binary using
`Makefile.bootstrap`.
